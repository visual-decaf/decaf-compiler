#include "parser_impl.h"
#include <iostream>
#include <vector>
using namespace std;
int main() {
    // yy::parser parser;
    // parser.parse();
    // auto tok = yylex();
    // cout << tok << endl;
}